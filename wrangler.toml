name = "mycivix"
pages_build_output_dir = "."
compatibility_date = "2026-08-24"

# For local testing only (`wrangler pages dev`). Production and preview
# bindings for the deployed Pages project are set in the Cloudflare
# dashboard under Settings -> Functions, separately per environment —
# this file has no effect on those.
[[kv_namespaces]]
binding = "DIG_KV"
id = "51a8027e54724bfb8b6cb34d3ae10ec3"
preview_id = "c1e1b859de3a4685b112c23f340be38c"
